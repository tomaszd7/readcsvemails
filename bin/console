#!/usr/bin/env php
<?php
use Console\ValidateEmailsSymfonyCommand;

require_once __DIR__ . '/../vendor/autoload.php';
 
use Symfony\Component\Console\Application;
 
$app = new Application();
$app->add(new ValidateEmailsSymfonyCommand());
$app->run();